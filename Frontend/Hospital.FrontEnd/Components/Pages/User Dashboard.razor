@page "/userdashboard"
@using Hospital.FrontEnd.Components.Layout
@layout NavMenu4
@using Hospital.FrontEnd.Models
@using Hospital.FrontEnd.Clients
@using Microsoft.VisualBasic


@inject AppointmentClient appointmentClient
@inject IJSRuntime JS
@rendermode @(new InteractiveServerRenderMode(prerender: false))


<div class="container-fluid mt-3">
        <div class="row justify-content">
            <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                           <i class="fa fa-calendar me-2" aria-hidden="true"></i>
                            <span>Upcoming Appointments</span>
                        </div>
                        <div class="card-body">
                            @if(isLoadingAppointments)
                            {
                                <p class="text-center">Loading your appointments..</p>
                            }
                            else if(appointments == null || !appointments.Any())
                            {
                                <p class="text-muted text-center">you have no appointments yet</p>
                            }
                            else
                            {
                            <div class="mb-6">
                               @foreach (var apt in appointments.OrderByDescending(a => a.DateTime))
                                    {
                                       <div class="mb-3">
                                            <input type="text" name="title" placeholder="" id="title" required class="form-control" value="@($"{apt.DoctoreName} - {apt.DateTime.ToString("dd MMM yyyy")}")">
                                        </div>
                                    }
                            </div>  
                            }
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <i class="fa fa-file-text me-2" aria-hidden="true"></i>
                             <span>Medical Records</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                           <i class="fa fa-credit-card-alt me-2" aria-hidden="true"></i>
                            <span>MY Bills</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
             <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                           <i class="fa fa-flask me-2" aria-hidden="true"></i>
                            <span>Lab Results</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mt-2">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                           <i class="fa fa-calendar me-2" aria-hidden="true"></i>
                            <span>Book An Appointment</span>
                        </div>
                        <div class="card-body">
                            @if (!string.IsNullOrEmpty(successMessage)) { <div class="alert alert-success">@successMessage</div> }
                            @if (!string.IsNullOrEmpty(errorMessage)) {  <div class="alert alert-danger">@errorMessage</div>}
                            <EditForm Model="newAppointment" OnValidSubmit="HandleBookAppointment" FormName="Appointment">
                                <DataAnnotationsValidator />
                                <ValidationSummary />
                                <div class="mb-3">
                                    <InputText @bind-Value="newAppointment.DoctoreName"  class="form-control"  placeholder="Enter Doctor" />
                                    <ValidationMessage TValue="string" For="() => newAppointment.DoctoreName"/>
                                </div>

                                <div class="mb-3">
                                    <InputText @bind-Value="newAppointment.DepartmentName"  class="form-control"  placeholder="Enter Department" />
                                    <ValidationMessage TValue="string" For="() => newAppointment.DepartmentName"/>
                                </div>

                                <div class="mb-3">
                                    <InputDate @bind-Value="newAppointment.DateTime"  class="form-control" />
                                </div>

                                <button type="submit"   class="btn btn-primary w-100 mt-3"  disabled="@isLoading">
                                    @(isLoading ? "Booking Appointment..." : "Book Appointment")
                                </button>
                            </EditForm>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-5 mt-2">
                <div class="card card-teal mt-3">
                    <div class="card-header">
                        <i class="fa fa-heart"></i>
                        <span>Health Tips</span>
                    </div>
                    <div class="card-body p-0">
                        <img src="./images/HealthTips2.jpg" style="width: 100%; height: 250px; object-fit: cover;">
                    </div>
                </div>
            </div>
             <div class="col-md-3 mt-2">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <span>Recent Reports</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6 ">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                                <div class="mb-6 mt-3">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-9">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <span>My Appointments</span>
                        </div>
                        <div class="card-body">
                            @if (isLoadingAppointments)
                                {
                                    <p class="text-center">Loading your appointments...</p>
                                }
                                else if (appointments == null || !appointments.Any())
                                {
                                    <p class="text-muted text-center">You have no appointments yet.</p>
                                }
                                else
                                {
                           <div class="table-responsive">
                            <table class="table table-hover">
                                <tbody>
                                    @foreach (var apt in appointments.OrderByDescending(a => a.DateTime))
                                    {
                                        <tr>
                                            <td><strong>@apt.DoctoreName</strong></td>
                                            <td>@apt.DepartmentName</td>
                                            <td>@apt.DateTime.ToString("dd MMM yyyy  hh:mm tt")</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <span>Billing Summary</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6 ">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
             <div class="col-md-9">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <span>Notification</span>
                        </div>
                        <div class="card-body">
                                <div class="mb-6 mt-3">
                                    <input type="text" name="title" placeholder="" id="title" required class="form-control" value="">
                                    <div class="text-danger"></div>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card mt-3">
                    <div class="card card-teal">
                        <div class="card-header">
                            <span>Hospital Info</span>
                        </div>
                        <div class="card-body">
                            <div class="mb-6 ">
                                <label for="Info" style="color: rgb(227, 13, 42);"> 📍 Location : Talagala Rd, Pannipitiya </label>
                            </div>
                             <div class="mb-6 mt-2">
                                <label for="Info" style="color: rgb(227, 13, 42);"> ☎️ (112) 001122 </label>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</div>

@code {
    private CreateAppointment newAppointment = new();
    private List<Appointment>? appointments;
    private string? successMessage;
    private string? errorMessage;
    private bool isLoading = false;
    private bool isLoadingAppointments = true;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await LoadMyAppointments();
            isLoadingAppointments = false;
            StateHasChanged();
        }
    }

    private async Task HandleBookAppointment()
    {
        successMessage = null;
        errorMessage = null;
        isLoading = true;

        try
        {
            var token = await JS.InvokeAsync<string>("localStorage.getItem", "token");

            if (string.IsNullOrWhiteSpace(token))
            {
                errorMessage = "You are not logged in. Please login again.";
                return;
            }

            var response = await appointmentClient.CreateAppointmentAsync(newAppointment, token);

            if (response.IsSuccessStatusCode)
            {
                successMessage = "✅ Appointment booked successfully!";
                newAppointment = new CreateAppointment();
                await LoadMyAppointments();
            }
            else
            {
                errorMessage = "Failed to book appointment. Please try again.";
            }
        }
        catch (Exception ex)
        {
            errorMessage = $"Error: {ex.Message}";
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task LoadMyAppointments()
    {
        try
        {
            var token = await JS.InvokeAsync<string>("localStorage.getItem", "token");

            if (!string.IsNullOrWhiteSpace(token))
            {
                appointments = await appointmentClient.GetMyAppointmentsAsync(token);
                
            }
            else
            {
                appointments = new();
            }
        }
        catch
        {
            appointments = new();
        }
    }
}